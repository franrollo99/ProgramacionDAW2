// Buscar dato del pokemon dependiendo del numero o nombre
function buscarPokemon(contenedor){
    let inputId = 'pokemonInput' + contenedor;
    let nombrePokemon = document.getElementById(inputId);
}