function pintaRombo(diagonal){
    let rombo=[];
    for(let i=0; i<diagonal; i++){
        let fila=[];
        for(let col=0; col<fila+diagonal; col++){
            fila[col]=(col<diagonal-fila-1);
        }
}